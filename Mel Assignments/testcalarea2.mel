proc float Add(float $num[]){    
        float $amount;
        $amount = $num[0] + $num[1] + $num[2];
        
        return $amount;
}

    float $val[] = {1,2,4};
    
    $sum = Add($val);

proc cal(string $input){
         
    if($input == "+"){
    print ($val[0] + " + " + $val[1]  + " + " +  $val[2] + " = " + $sum + "\n");
    }
    
}
cal("+");

